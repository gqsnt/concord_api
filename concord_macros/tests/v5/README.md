# Concord v5 macro fixture layout

Naming conventions:

- `pass_*`: should compile.
- `fail_*`: should fail with diagnostics.
- `snapshot_*`: should emit stable resolved/generated output.

M0-M2 fixtures are active and must not be ignored.
